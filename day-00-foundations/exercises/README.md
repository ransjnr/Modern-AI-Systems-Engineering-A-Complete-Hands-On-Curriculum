# Day 0 Exercises
Do every one; solutions are intentionally omitted.

1. **Python:** write `word_count(text) -> dict[str,int]`.
2. **Python:** from `[5,2,9,1,7]`, comprehension for even-indexed values.
3. **Terminal:** create `~/sprint/day0/`, add `notes.txt`, list it.
4. **Git:** init, commit, make a second commit, show `git log --oneline`.
5. **ML:** change the noise in `tiny_model.py`; explain the MAE change.
6. **API:** modify `call_api.py` to print open-issues count.
7. **API:** add `GET /reverse/{text}` to `mini_server.py`.
