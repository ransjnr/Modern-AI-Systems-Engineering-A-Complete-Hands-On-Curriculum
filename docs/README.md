# docs/

Place the day-by-day manual (`.docx`) files here alongside the code, or link to them.

The manuals are the *what and why*; this repository is the *runnable how*. Read the day's
chapter, then build the matching `day-XX-*/project-NN-*/` here.
